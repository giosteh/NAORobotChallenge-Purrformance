# NAORobotChallenge-Purrformance

Landmarks:
- use the codebase from AIMA book (Problem/PlanningProblem classes)
- model classes for: robot interface, (poses/gestures, ...)
- extend the problem class from AIMA
- use interpolation between moves specifying transition time
